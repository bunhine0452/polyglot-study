pub fn component_ids(n: usize, adj: &Vec<Vec<usize>>) -> Vec<usize> {
    // 정점을 0..n 순서로 훑어라. 아직 번호가 없는 정점을 만나면
    // 거기서부터 탐색을 시작해 닿는 정점 전부에 같은 번호를 매기고, 다음 번호로 넘어가라.
    unimplemented!("여기를 구현해라")
}
