pub fn dfs_order_iterative(n: usize, adj: &Vec<Vec<usize>>, start: usize) -> Vec<usize> {
    // Vec 을 스택처럼 써라: push 로 넣고 pop 으로 가장 최근 것부터 꺼낸다.
    // pop 한 직후에 방문했는지 확인하고 표시해라 — 이미 스택에 두 번 들어갔을 수 있다.
    unimplemented!("여기를 구현해라")
}
