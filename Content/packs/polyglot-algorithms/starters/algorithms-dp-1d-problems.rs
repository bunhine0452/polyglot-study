pub fn min_cost_climbing(cost: &[u32]) -> u32 {
    // dp[i]를 "i번째 계단(맨 위 포함)까지 오르는 최소 비용"으로 정의해라.
    // dp[0] = dp[1] = 0 (둘 다 무료로 시작할 수 있다).
    // dp[i] = min(dp[i-1] + cost[i-1], dp[i-2] + cost[i-2]).
    unimplemented!("여기를 구현해라")
}
