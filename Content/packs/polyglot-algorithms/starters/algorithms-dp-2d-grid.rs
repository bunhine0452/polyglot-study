pub fn min_path_sum(grid: &[Vec<u32>]) -> u32 {
    // dp[r][c]를 "(0,0)에서 (r,c)까지 오는 최소 비용"으로 정의해라.
    // 첫 행은 왼쪽에서만, 첫 열은 위에서만 올 수 있다.
    // 그 외 칸은 dp[r-1][c] 와 dp[r][c-1] 중 작은 쪽에 grid[r][c] 를 더해라.
    unimplemented!("여기를 구현해라")
}
