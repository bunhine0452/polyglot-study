pub fn count_ways(n: u32) -> u64 {
    // dp[0] = 1 (아무것도 안 더한 빈 합)에서 시작해라.
    // dp[i]는 마지막에 1을 더한 경우 + 3을 더한 경우 + 4를 더한 경우의 합이다(i가 그만큼 작을 때만).
    // 작은 인덱스부터 차례로 dp를 채워라.
    unimplemented!("여기를 구현해라")
}
