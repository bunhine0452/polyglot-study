pub fn build_adjacency_list(n: usize, edges: &[(usize, usize)]) -> Vec<Vec<usize>> {
    // 정점 n개짜리 빈 리스트를 만들고, 간선마다 양쪽 정점의 이웃 목록에 서로를 추가해라.
    // 마지막에는 각 정점의 이웃 목록을 오름차순으로 정렬해라.
    unimplemented!("여기를 구현해라")
}
