pub fn shortest_path(n: usize, adj: &Vec<Vec<usize>>, start: usize, goal: usize) -> Option<Vec<usize>> {
    // dist 와 parent 를 함께 기록하며 BFS 를 돌려라.
    // goal 에 처음 도달했을 때 그 거리가 최단 거리다.
    // 도달하지 못하면 None, 도달했다면 parent 를 거슬러 올라가 뒤집은 경로를 돌려줘라.
    unimplemented!("여기를 구현해라")
}
