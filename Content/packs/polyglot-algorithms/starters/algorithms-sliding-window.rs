pub fn min_subarray_len(xs: &[i32], target: i32) -> Option<usize> {
    // left, right 두 포인터로 가변 윈도우를 만들어라.
    // right 를 늘리며 합을 더하고, 합이 target 이상이면 left 를 줄이며 최소 길이를 갱신해라.
    unimplemented!("여기를 구현해라")
}
