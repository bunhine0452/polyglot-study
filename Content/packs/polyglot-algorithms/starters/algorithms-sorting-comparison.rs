pub fn stable_sort_pairs(xs: &mut Vec<(i32, usize)>) {
    // (키, 원래 순서) 쌍을 키 기준으로 정렬하되, 같은 키를 가진 원소들의 원래 순서는 유지해야 한다.
    // 삽입 정렬처럼 '이미 정렬된 부분'에 밀어 넣는 방식을 쓰고, 키가 같으면 밀지 마라.
    unimplemented!("여기를 구현해라")
}
