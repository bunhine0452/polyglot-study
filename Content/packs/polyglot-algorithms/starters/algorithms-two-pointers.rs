pub fn two_sum_sorted(xs: &[i32], target: i32) -> Option<(usize, usize)> {
    // lo 는 0, hi 는 xs.len() - 1 에서 시작해 서로를 향해 좁혀가라.
    // 합이 target 보다 작으면 lo 를, 크면 hi 를 옮겨야 새로운 후보를 볼 수 있다.
    unimplemented!("여기를 구현해라")
}
