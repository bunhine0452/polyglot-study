#include <algorithm>
#include <vector>
#include <stdexcept>

void analyze(std::vector<int> values, int target, int& outGreaterCount, int& outSortedIndex) {
    // outGreaterCount: target 보다 큰 원소 개수 (정렬 전 기준)
    // 그 다음 values 를 정렬하고 target 이 처음 나오는 인덱스를 outSortedIndex 에.
    // 없으면 -1.
    throw std::runtime_error("여기를 구현해라");
}
