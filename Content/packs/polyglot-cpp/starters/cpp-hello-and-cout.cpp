#include <string>
#include <stdexcept>

std::string greeting(const std::string& name) {
    // name 이 비어 있으면 "이름 없음" 을 대신 쓴다.
    // "안녕하세요, " + 이름 + "!" 를 이어 붙여 돌려준다.
    throw std::runtime_error("여기를 구현해라");
}
