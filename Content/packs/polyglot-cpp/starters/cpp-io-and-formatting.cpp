#include <sstream>
#include <iomanip>
#include <string>
#include <stdexcept>

std::string formatFixed(double value) {
    // value 를 소수점 둘째 자리까지 고정해 문자열로 반환한다.
    throw std::runtime_error("여기를 구현해라");
}
