#include <vector>
#include <stdexcept>

std::vector<int> filterAndSort(const std::vector<int>& values, int limit) {
    // limit 보다 작은 원소만 골라 새 벡터에 담고 오름차순 정렬해 돌려준다.
    // 람다로 조건을 표현해 보라: auto isUnder = [limit](int x) { return x < limit; };
    throw std::runtime_error("여기를 구현해라");
}
