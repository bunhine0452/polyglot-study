#include <map>
#include <string>
#include <vector>
#include <stdexcept>

std::map<std::string, int> countWords(const std::vector<std::string>& words) {
    // words 를 순회하며 map 에 등장 횟수를 누적한다.
    throw std::runtime_error("여기를 구현해라");
}
