#include <vector>
#include <stdexcept>

void findMinMax(const std::vector<int>& values, int& outMin, int& outMax) {
    // values 가 비었으면 std::invalid_argument 를 던진다.
    // 그렇지 않으면 outMin 과 outMax 에 최솟값·최댓값을 채운다.
    throw std::runtime_error("여기를 구현해라");
}
