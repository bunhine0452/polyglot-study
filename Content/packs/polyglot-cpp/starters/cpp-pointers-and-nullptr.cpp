#include <vector>
#include <stdexcept>

int* findFirstNegative(std::vector<int>& numbers) {
    // numbers 를 순회하며 처음 나오는 음수의 주소를 돌려준다.
    // 음수가 없으면 nullptr 를 돌려준다.
    throw std::runtime_error("여기를 구현해라");
}
