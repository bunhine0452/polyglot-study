#include <vector>
#include <numeric>
#include <algorithm>
#include <stdexcept>

int sumOfSquares(const std::vector<int>& values) {
    // 각 원소의 제곱을 더한 합을 accumulate 로 구한다.
    throw std::runtime_error("여기를 구현해라");
}

std::vector<int> incrementAll(const std::vector<int>& values, int amount) {
    // 각 원소에 amount 를 더한 새 벡터를 transform 으로 만든다.
    throw std::runtime_error("여기를 구현해라");
}
