#include <string>
#include <stdexcept>

std::string compareResult(int actual, int expected) {
    // actual 과 expected 가 같으면 "일치" 를 돌려준다.
    // 다르면 "기대값 X, 실제값 Y" 형식으로 돌려준다.
    throw std::runtime_error("여기를 구현해라");
}
