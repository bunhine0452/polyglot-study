def ticket_price(age, is_student):
    # 요금 규칙:
    # - age가 8 미만이면 0 (음수 나이도 8 미만으로 처리)
    # - age가 65 이상이면 3000 (학생 여부와 관계없이)
    # - age가 8 이상 65 미만이고 학생이면(is_student가 True이면) 5000
    # - 그 외에는 8000
    # 위 규칙에 따라 요금을 정수로 반환하세요.
    pass
