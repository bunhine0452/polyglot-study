def save_and_load(lines, filename):
    # lines의 각 문자열을 한 줄로 해서 filename 파일에 저장하고,
    # 같은 파일을 다시 읽어 들여 줄 단위 리스트로 반환하세요.
    # 1. with 블록으로 filename 파일을 "w" 모드로 열기
    # 2. "\n".join(lines)의 결과를 write()로 기록하기
    # 3. 다시 with 블록으로 같은 파일을 "r" 모드로 열어 read()로 읽기
    # 4. 읽은 문자열에 splitlines()를 적용해 돌려주기
    pass
