def summary(text):
    # text 의 양쪽 공백을 strip() 으로 제거한 뒤,
    # 남은 문자열을 upper() 로 대문자로 바꾸고 len() 으로 길이를 구해서
    # f-string 으로 "[단어] len=길이" 형태를 만들어 반환하세요.
    # 예: summary("  hi  ") -> "[HI] len=2"
    ...
