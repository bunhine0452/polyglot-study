pub fn top_n_by_score(mut people: Vec<(String, i32)>, n: usize) -> Vec<String> {
    // people 을 점수(튜플의 두 번째 원소) 오름차순으로 정렬한 뒤 뒤집으면 내림차순이 된다.
    // 앞에서부터 n 개만 취해 이름만 뽑아 Vec<String> 으로 모아라.
    unimplemented!("여기를 구현해라")
}
