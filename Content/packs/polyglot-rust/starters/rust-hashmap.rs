use std::collections::HashMap;

pub fn word_counts(words: &Vec<&str>) -> Vec<(String, i32)> {
    // HashMap 의 entry API 로 각 단어의 등장 횟수를 센 뒤,
    // (단어, 횟수) 쌍으로 만들어 단어 사전순으로 정렬해 돌려준다.
    unimplemented!("여기를 구현해라")
}
