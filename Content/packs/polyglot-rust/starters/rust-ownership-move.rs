pub fn build_pair(word: String) -> String {
    // word 를 combined 로 옮기기 전에 clone 으로 backup 을 떠 두고,
    // combined 에 "!" 를 push_str 한 뒤 format! 으로 "combined / backup" 을 만들어라.
    unimplemented!("여기를 구현해라")
}
