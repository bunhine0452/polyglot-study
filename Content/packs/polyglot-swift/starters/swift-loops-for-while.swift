func sumSkipMultiplesForIn(from: Int, to: Int, step: Int) -> Int {
    // for-in 문과 continue 로 구현하라
    fatalError("여기를 구현해라")
}

func sumSkipMultiplesWhile(from: Int, to: Int, step: Int) -> Int {
    // while 문으로 같은 일을 구현하라
    fatalError("여기를 구현해라")
}
