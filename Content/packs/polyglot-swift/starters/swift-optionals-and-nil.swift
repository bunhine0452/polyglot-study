func lookupAge(_ ages: [String: Int], name: String) -> String {
    // ages[name] 의 결과는 옵셔널입니다.
    // if let 으로 값을 꺼내 "\(name): \(age)살" 을 반환하고,
    // nil 이면 "\(name): 나이를 모릅니다" 를 반환하세요.
    fatalError("여기를 구현해라")
}
