func totalScore(_ scores: [Int]) -> Int {
    // 점수 배열의 총합을 reduce 로 구해 반환하라
    fatalError("여기를 구현해라")
}

func parseScores(_ raws: [String?]) -> [Int] {
    // nil 이 아니고 Int 로 변환 가능한 원소만 골라 반환하라
    fatalError("여기를 구현해라")
}

func topScores(_ scores: [Int], count: Int) -> [Int] {
    // 내림차순으로 정렬해 앞에서 count 개만 반환하라
    fatalError("여기를 구현해라")
}
